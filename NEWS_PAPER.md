# ニュースとチャートの仮想判断・追跡

API通信も実注文も行いません。Python環境にrequirements-paper.txtを追加済みです。
他のPCでは `python -m pip install -r requirements-paper.txt` を先に実行してください。

## 登録済みの実資料3件で動作確認

```bat
python news_paper.py record --ai-db data/news_eval_ai.sqlite3 --ledger data/news_paper_test.sqlite3 --test-data
python news_paper.py report --ledger data/news_paper_test.sqlite3
```

古い資料の転記には正確な公表日時が登録されていないためニュース条件は見送りになります。
株価の将来データがまだない期間は待機中。テスト資料は本番集計から除外します。
原資料の発表日に判断した扱いにはしません。

## 今後のニュースで観測

新しい本文の公表日時を確認して登録・分析し、Yahooの日足を更新した後に実行します。

```bat
python yahoo_history.py
python news_paper.py record
python news_paper.py report
```

recordはコマンドを実行した現在時刻の判断を保存します。取得・分析完了後であることを検査し、
日足も保存取得日時が現在以前、かつ前日までのものだけを使用します。当日足は使いません。
同じ記事・銘柄・ルールは一度だけ記録し、再分析・後日の再実行で初回の判断を上書きしません。
最新分析が失敗・未完了なら対象外。複数企業の記事は保留。別URLの同一イベント重複は未対応です。

チャート条件：終値>20日平均、5日平均>20日平均、出来高>=直前20本平均。
直前21営業日の欠損、7暦日超の古い株価は見送り。
ニュース併用条件：公表から7日以内、企業自身への直接関連、品質警告なし、
同じ期間の営業利益の変更前が正かつ変更後が増加した一組の数値。カテゴリ名は条件に使いません。
これは検証用に決めた固定条件であり、有効性を確認した戦略ではありません。

## 騰落率の定義

- 判断日の翌営業日の始値で仮想購入。日中・休日の実行でも判断当日の価格では買いません。
- 1/5/20営業日は購入日を1日目とし、その日の終値までの騰落率。
- exchange_calendarsのXTKSカレンダーと版、予定日を判断時点に固定保存。
- 評価も前日までの確定日足を使い、必要日が未来なら待機中、欠けていれば価格欠損。
- 分割・併合は取得元の係数で補正。売買各5bpsの滑りを仮定。配当・税・手数料は含みません。
- 日足後日訂正で評価が変わる可能性があります。各レポートに使った価格とハッシュを保存します。
- カレンダーは外部ライブラリの予定。臨時休場や銘柄固有の売買停止の約定を保証しません。

同じニュース観測対象についてチャートのみとニュース併用を比較します。
候補数・評価済み数・未評価数・候補の平均騰落率を表示します。
見送りの銘柄も参考価格推移は記録しますが、購入成績に含めません。
同時保有、資金制約、ロット、指値等は未実装。平均騰落率は口座収益率ではありません。
日々の全銘柄を対象にしたチャート戦略との比較でもありません。
実運用成績ではなく前向きな条件検証の記録です。
